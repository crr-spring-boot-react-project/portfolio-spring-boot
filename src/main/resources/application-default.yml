spring:
  jpa:
    database: h2
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: false

  # 🚨 1. datasource 설정을 spring 블록 하위로 이동 🚨
  datasource:
    url: jdbc:h2:mem:portfolio
    username: sa
    password:
    driver-class-name: org.h2.Driver

  # 🚨 2. h2 설정을 spring 블록 하위로 이동 🚨
  h2:
    console:
      enabled: true
      path: /h2-console